# API Keys for paper pipeline
# Copy this file to .claude/commands/.env and fill in your key:
#   cp .env.example .claude/commands/.env

# OpenAI API key — used by 07-write-paper for figure generation (DALL-E / GPT-4o image)
OPENAI_API_KEY=sk-...

# Optional: Anthropic API key (if using Claude API directly)
# ANTHROPIC_API_KEY=sk-ant-...
